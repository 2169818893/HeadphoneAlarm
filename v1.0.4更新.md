# 耳机闹钟 v1.0.4 更新说明（待发版）

本文记录相对于 v1.0.3 的本轮代码审查与修复。**目前工程尚未正式升版**：app/build.gradle.kts 中仍为 versionName = "1.0.3"、versionCode = 4；已有的 Debug APK 是验证构建产物，不应作为 v1.0.4 正式安装包发布。正式发布前需更新版本号并重新构建、测试。

## 主要修复

- **闹钟触发与排程**：区分普通闹钟和贪睡的系统触发请求，避免二者互相覆盖；修复一次性闹钟触发后的消费逻辑，以及多个闹钟先后响起时的处理；系统闹钟图标的入口改为打开应用主页，而不是未响铃的响铃页。
- **贪睡可靠性**：将贪睡目标时间持久化，退出应用或设备重启后可恢复有效的贪睡；取消、删除、编辑或关闭闹钟时同步处理旧贪睡，并校验触发令牌，避免过期或已取消的请求误响。重复闹钟与其贪睡可以各自排程。
- **数据与系统状态一致性**：集中串行执行闹钟数据写入和系统排程，避免异步操作交错造成闹钟状态与 AlarmManager 不一致；记录注册/取消失败并在界面提示，不再将失败当成成功。
- **响铃服务与锁屏交互**：强化前台服务的启动、停止、唤醒锁和多次触发时的生命周期处理，减少后台、锁屏和快速连续操作导致的漏响或错误关闭。
- **耳机音频安全**：仅耳机模式在播放前及播放中确认真实输出路由，对无法明确识别为耳机的设备保守处理，以降低误外放风险；修复音量调整后在用户手动调节、系统限幅或输出设备变化时错误恢复旧媒体音量的问题。**用户主动选择“允许外放”时，仍可通过扬声器响铃。**
- **自定义铃声**：改进导入、试听、替换和删除流程，对不再被闹钟引用的私有铃声文件进行安全清理，避免遗留文件或误删仍在使用的文件。
- **时间与系统事件**：修复跨夏令时计算下一次响铃时间的边界情况；开机、应用更新以及系统时间/时区变化后重排闹钟，并恢复仍然有效的贪睡。

## 界面与使用体验

- 列表和编辑页面改进数据读取、保存及排程失败时的提示，避免静默失败或显示误导性的成功状态。
- 列表展示下一次有效触发时间时考虑贪睡；一次性闹钟触发后即使常规开关关闭，待执行的贪睡仍可被识别。
- README 更新了仅耳机模式的适用范围：通用 USB 音频设备不一定是耳机；某些无麦 Type-C 耳机如仅被系统识别为通用 USB 设备，可能保持静音，而不是冒险外放。

## 验证情况

- Debug 单元测试：**32 项通过**（音量恢复 5 项、闹钟数据与时间 17 项、时间工具 10 项）。
- Debug APK：**构建成功**，测试产物位于 app/build/outputs/apk/debug/app-debug.apk。
- Android Lint：**0 errors、7 warnings**；警告尚未全部消除。

## 发布前注意事项

1. **尚未完成真机验证**：请在目标设备上覆盖耳机插拔、蓝牙/USB 输出路由、息屏锁屏响铃、多个闹钟连续触发和设备重启后的贪睡恢复等场景。
2. 仅耳机模式的防外放依赖 Android/设备正确报告实际音频路由，不能保证所有定制系统或外接设备绝对不会外放；重要日程建议设置备用闹钟。
3. 发布 v1.0.4 前，先调整工程的 versionName、versionCode，重新构建并做完整回归；不要把当前 Debug APK 直接标记为 v1.0.4 发布包。
